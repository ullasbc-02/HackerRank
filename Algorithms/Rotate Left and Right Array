#Rotate Left
arr=[8,6,4,9,2]
temp=arr[0]
for i in range(len(arr)-1):
    arr[i]=arr[i+1]
arr[len(arr)-1]=temp
print(arr)

#Rotate Right
temp=arr[len(arr)-1]
for i in range(len(arr)-1,0,-1):
    arr[i]=arr[i-1]
arr[0]=temp
print(arr)
