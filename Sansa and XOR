def sansaXor(arr):
    # Write your code here
    if(len(arr)%2==0):
        return 0
    else:
        result = arr[0]
        for idx in range(2,len(arr),2):
            result = result ^ arr[idx]
        return result

#Occuring even number of times then the result is 0 i.e., a^a^a^a = 0
